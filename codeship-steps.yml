- name: build
  service: googleclouddeployment
  command: docker build -t app-builder -f Dockerfile.build /deploy
- name: run
  service: googleclouddeployment
  command: docker run -i app-builder > app-server.tar.gz
